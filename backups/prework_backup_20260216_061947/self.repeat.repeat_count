37:    "shift": "shift",
136:        if target_window_title is None:
173:        self.poll_interval: float = 0.0 if self.perf_mode else 0.1
181:            "image_branch": lambda sct, mon, s, idx: (*self._image_branch(sct, mon, s), 0), # Adapter for different signature
203:            "ocr_jump_if": lambda sct, mon, s, idx: self._ocr_jump_if(sct, mon, s),
208:            "jump_if": lambda sct, mon, s, idx: self._jump_if(s),
213:        if s.id not in self._loop_counters:
222:            "variable_context": dict(self.variable_context) if self.variable_context else {},
223:            "counters": dict(self._counters) if self._counters else {},
225:            "data_list": list(self._data_list) if self._data_list else None,
226:            "data_rows": list(self._data_rows) if self._data_rows else None,
227:            "data_row_values": list(self._data_row_values) if self._data_row_values else None,
228:            "data_columns": list(self._data_columns) if self._data_columns else [],
231:            "loop_counters": dict(self._loop_counters) if self._loop_counters else {},
232:            "call_stack": list(self.call_stack) if self.call_stack else [],
240:        if self.isRunning():
246:        if resume_state is not None:
252:            if self.perf_mode:
254:                if hasattr(pyautogui, "MINIMUM_DURATION"):
256:                if hasattr(pyautogui, "MINIMUM_SLEEP"):
260:                if hasattr(pyautogui, "MINIMUM_DURATION"):
262:                if hasattr(pyautogui, "MINIMUM_SLEEP"):
272:        if resume_state is not None:
275:            self._run_started_at = started_at if isinstance(started_at, (int, float)) else time.time()
288:            if steps is not None:
291:            if path is not None:
310:            if self.target_window_title:
313:                    if hwnd:
320:                    # Do not abort if activation fails
330:                    if self._max_duration_expired(run_started_at):
334:                    if self.repeat.repeat_count > 0 and loop_idx >= self.repeat.repeat_count:
338:                    if loop_idx > 0:
344:                    i = self.start_index if loop_idx == 0 else 0
349:                        if i >= len(self.steps):
350:                            if self.call_stack:
360:                        if self._stop: break
361:                        if self._max_duration_expired(run_started_at):
365:                        if hops > 2000:
371:                        if not self.call_stack:
387:                        if self._switch_steps is not None:
396:                        if not ok:
397:                            if self.capture_on_fail:
404:                            if step.branch_on_fail_goto_id:
406:                                if jump_to_idx is not None:
414:                            if self.repeat.stop_on_fail:
420:                            if fail_goto_id: # Actually success_goto_id in this context (e.g. from compare/branch)
422:                                if jump_to_idx is not None:
429:                        if self.poll_interval and self.poll_interval > 0:
434:                    if self.repeat.repeat_count > 0 and loop_idx >= self.repeat.repeat_count:
440:                        if self._max_duration_expired(run_started_at):
460:        if self.repeat.max_duration_ms > 0:
461:            if (time.time() - start_time) * 1000 > self.repeat.max_duration_ms:
462:                if not self._max_duration_fired:
469:        if hasattr(s, 'pre_delay_ms') and s.pre_delay_ms > 10:
476:        if handler:
477:            # Normalize signatures if needed, but for now we used lambdas in __init__ to adapt them
486:        if ms <= 0: return
489:            if self._stop: break
495:        # Simplified for brevity, but should include full logic
500:        if capture_region is None:
506:            if self._stop: return (False, None, 0)
511:            # Find all targets if requested
512:            if s.find_all_targets:
514:                if results:
517:                        if self._stop:
521:                        if self.dry_run:
529:            if mr.ok:
539:                if self.dry_run:
545:                if getattr(s, "loop_until_hide", False):
552:                        if mr2.ok:
556:                            if not self.dry_run:
566:                if getattr(s, "hold_until_next", False):
568:                    if next_img and not self._hold_until_next_image(sct, mon, s, next_img):
579:        if getattr(step, "branch_mode", "image") == "variable":
588:            if source == "ocr":
590:                if sct is None or mon is None:
599:                    if roi["width"] <= 0 or roi["height"] <= 0:
615:                    if getattr(step, "ocr_save_enabled", False):
617:                        if varname:
621:                ctx_val = self.variable_context.get(var_name, None) if var_name else None
624:            if ctx_val is None:
637:            goto = true_id if result else false_id
642:        if capture_region is None:
647:            if self._stop: return (False, None)
656:                # Note: If temp_step has a specific ROI, we are currently matching against the PARENT step's ROI capture.
661:                if mr.ok:
672:                    if not self.dry_run:
687:            if candidate.type == "image_click":
698:        if region_a is None:
700:        if region_b is None:
706:            if self._stop:
713:            if mrB and mrB.ok:
715:                if consecutive_matches >= release_consecutive:
720:            if (time.time() - last_click) * 1000 >= interval_ms:
722:                if reacquire or not getattr(current_step, "_last_match_xy", None):
726:                        if mrA and mrA.ok:
734:                if ax is not None and ay is not None:
735:                    if self.dry_run:
758:        if not getattr(temp_step, "threshold", None):
763:        if target.get("search_roi_enabled"):
780:        if action == "none":
784:        if s.jitter > 0:
793:        if s.pre_move_sleep_ms > 0:
800:        if btn == "double":
803:        if btn in ("move_only", "none", None, ""):
807:        if self.dry_run:
809:        elif self.human_mode:
813:            if action != "move":
814:                if double_click:
817:                        if duration > 0:
820:                        if tap == 0:
824:                    if duration > 0:
829:        if post_sleep > 0:
834:        if action in {"click", "move", "none"}:
837:        if legacy_button == "move_only":
839:        elif legacy_button == "none":
848:        if not text:
851:        mods, base = hk_to_tuple(normalized) if normalized else (set(), None)
853:        if mods or (base and (len(base) == 1 or base in PHYSICAL_KEY_NAMES)):
854:            if base and len(base) == 1 and not base.isascii():
857:        if len(text) == 1:
858:            if not text.isascii():
866:        if not txt:
871:            if not self._set_clipboard_text(txt):
875:            if not self.dry_run:
880:            if prev is not None:
913:        if not key:
925:        if key in aliases:
929:    def _press_with_modifiers(self, mods: set[str], base: str) -> bool:
931:        if not key_name:
934:        if self.dry_run:
938:                if mk:
943:                if mk:
949:        if enabled:
960:        if not data:
963:        if data["text"] is not None:
964:            if self.dry_run:
970:        if not base:
972:        pretty = hk_pretty(s.key_string) if s.key_string else base
975:            if not self._press_with_modifiers(data["mods"], base):
981:        if not data or data["text"] is not None or not data["base"]:
984:        if not key_name:
986:        if self.dry_run:
990:            if mod_name:
997:        if not data or data["text"] is not None or not data["base"]:
1000:        if not key_name:
1002:        if self.dry_run:
1007:            if mod_name:
1013:        if not data or data["text"] is not None or not data["base"]:
1016:        if not key_name:
1018:        if self.dry_run:
1023:                if mod_name:
1031:                if mod_name:
1037:        if mode == "text":
1039:        if mode == "key":
1041:        if mode == "key_down":
1043:        if mode == "key_up":
1045:        if mode == "key_hold":
1052:        if mode == "drag":
1054:        if mode == "scroll":
1056:        if mode == "click_point":
1063:        if mode == "ocr_check_text":
1065:        if mode == "pixel_check":
1072:        if mode == "run_macro":
1075:        if mode == "load_data_file":
1081:        if s.click_x is None or s.click_y is None: return (False, None)
1082:        if self.dry_run:
1087:            if self.human_mode:
1091:                if s.click_double:
1101:        if None in (s.drag_from_x, s.drag_from_y, s.drag_to_x, s.drag_to_y):
1103:        if self.dry_run:
1109:            if self.human_mode:
1122:        if s.click_x is None or s.click_y is None:
1124:        if self.dry_run:
1128:            if self.human_mode:
1141:            if s.click_x is not None and s.click_y is not None:
1142:                if self.dry_run:
1145:                    if self.human_mode:
1149:            if self.dry_run:
1151:            if self.human_mode:
1154:                if double:
1165:        if len(path) < 2:
1174:                    pt = float(node[2]) if len(node) > 2 else None
1178:        if len(points) < 2:
1180:        if self.dry_run:
1186:            if self.human_mode:
1193:                    if prev_t is not None and ts is not None:
1195:                        if delta > 0.0:
1207:        if duration > 0:
1228:        if s.pixel_x is None or s.pixel_y is None or not s.pixel_color_hex:
1234:            diff_r = abs(target_rgb[0] - screen_rgb[0])
1235:            diff_g = abs(target_rgb[1] - screen_rgb[1])
1236:            diff_b = abs(target_rgb[2] - screen_rgb[2])
1237:            if diff_r <= tolerance and diff_g <= tolerance and diff_b <= tolerance:
1255:            if not path_a or not path_b:
1258:            # Load images (simplified)
1260:            # For now using standard cv2.imread (might fail with unicode paths on Windows if not handled)
1271:            if img_a is None or img_b is None:
1276:            if roi_w > 0 and roi_h > 0:
1283:                if ax2 <= roi_x or ay2 <= roi_y or bx2 <= roi_x or by2 <= roi_y:
1293:                if img.ndim == 3 and img.shape[-1] == 3:
1300:                if a.shape != b.shape:
1311:                if den == 0:
1318:                diff = resized[:, 1:] > resized[:, :-1]
1319:                return diff.flatten()
1322:            if mode == "mse":
1323:                if img_a.shape != img_b.shape:
1328:            elif mode == "ssim":
1329:                if img_a.shape != img_b.shape:
1334:            elif mode == "hash":
1343:            return (True, s.on_match_goto_id) if is_match else (False, None)
1350:        if not save_path:
1355:            if dir_name and not os.path.exists(dir_name):
1357:            if s.screenshot_roi_w > 0 and s.screenshot_roi_h > 0:
1375:            if region["width"] <= 0 or region["height"] <= 0:
1378:            if hasattr(grab, "rgb"):
1389:            if is_success:
1401:        if target_h > 0 and img_bgr is not None:
1406:            if h > 0:
1408:        if scale <= 0:
1413:        if img_bgr is None:
1415:        if img_bgr.ndim == 3:
1422:        if mode in ("none", "no", "off"):
1424:        elif mode in ("blur", "gaussian"):
1427:        elif mode in ("adaptive", "adapt"):
1434:        if invert:
1439:        if img_bgr is None:
1449:        if h > 0 and w > 0 and scale != 1.0:
1460:        if whitelist:
1462:            if wl:
1466:            if config:
1475:            if s.ocr_roi_w > 0 and s.ocr_roi_h > 0:
1486:            if hasattr(raw, "rgb"):
1492:            if img_bgra.ndim == 3 and img_bgra.shape[-1] == 4:
1494:            elif img_bgra.ndim == 3 and img_bgra.shape[-1] == 3:
1497:                # Fallback: create blank image if grab is unexpected
1510:            if getattr(s, "ocr_use_dynamic_data", False):
1517:            if expected:
1523:                if val is not None and exp_val is not None and abs(val - exp_val) < 1e-6:
1528:                    if raw_text and expected.lower() in raw_text.lower():
1530:                goto = s.on_match_goto_id if ok else None
1533:                goto = s.on_match_goto_id if ok else None
1537:                col = (0, 255, 0) if ok else (255, 0, 0)
1542:            # Store in context if var_name provided
1544:            if var_name:
1552:    def _ocr_jump_if(self, sct, mon, s: StepData) -> tuple[bool, str | None, int]:
1553:        if sct is None or mon is None:
1557:            if s.ocr_roi_w > 0 and s.ocr_roi_h > 0:
1568:            if hasattr(raw, "rgb"):
1574:            if img_bgra.ndim == 3 and img_bgra.shape[-1] == 4:
1576:            elif img_bgra.ndim == 3 and img_bgra.shape[-1] == 3:
1590:            if compare_to not in (None, ""):
1597:            if use_text:
1599:                actual_value = raw_text.strip() if raw_text else ""
1601:            if actual_value in (None, ""):
1611:                col = (0, 255, 0) if is_true else (255, 0, 0)
1616:            if is_true:
1618:                jump_idx = getattr(s, "target_true_index", None) if hasattr(s, "target_true_index") else getattr(s, "jump_to_index", None)
1621:                if jump_id:
1623:                        if getattr(st, "id", None) == jump_id:
1628:                if goto_id is None and jump_idx is not None:
1631:                        if 0 <= jump_idx < len(self.steps):
1637:                if goto_id is None:
1638:                    self.log.emit(f"  !! ocr_jump_if: condition true but target not found (id={jump_id}, idx={jump_idx}).")
1652:        if not var_name:
1659:        if val is None:
1661:                if s.ocr_roi_w > 0 and s.ocr_roi_h > 0:
1672:                if img_bgra.shape[-1] == 4:
1693:        if not target_path:
1697:        if len(self.call_stack) >= self._max_call_depth:
1702:        if not path_obj.exists():
1707:        if not new_steps:
1728:        if not path:
1731:        if len(self.call_stack) >= self._max_call_depth:
1736:            if not new_steps:
1757:    def _jump_if(self, s: StepData) -> tuple[bool, str | None, int]:
1762:        if not var_name:
1763:            self.log.emit("  !! jump_if: condition_var not set.")
1766:        if var_name not in self.variable_context:
1767:            self.log.emit(f"  !! jump_if: variable '{var_name}' not found. Continue.")
1775:                self.logger.error("jump_if evaluation failed; treating as FALSE.", exc_info=True)
1780:        if is_true:
1782:            jump_idx = getattr(s, "target_true_index", None) if hasattr(s, "target_true_index") else getattr(s, "jump_to_index", None)
1786:            if jump_id:
1788:                    if getattr(st, "id", None) == jump_id:
1794:            if goto_id is None and jump_idx is not None:
1797:                    if 0 <= jump_idx < len(self.steps):
1804:            if goto_id is None:
1805:                self.log.emit(f"  !! jump_if: condition true but target not found (id={jump_id}, idx={jump_idx}).")
1815:        if not path: return False
1819:        if not success:
1823:        if not data:
1829:        # Convert list of dicts to list of lists (values only) for compatibility if needed,
1837:        self._data_list = [list(row.values())[0] for row in data if row]
1846:            if path_obj.suffix.lower() == ".macro":
1853:            steps_data = data.get("steps", data if isinstance(data, list) else [])
1871:        if not text:
1878:        if path_obj.is_absolute():
1881:        if self.current_file_path:
1886:        if base:
1892:        if not start_id or start_id not in self._loop_counters:
1897:        if self._data_list is not None:
1899:            if self._data_index < len(self._data_list):
1903:        if count == 0: # infinite
1906:        if count > 1:
1915:        # Simplified implementation
1918:            if name not in self._counters:
1930:            if key not in self._counters:
1940:            if t == "#":
1942:            if t == "@":
1944:            if t == "?":
1952:            if name in self.variable_context:
1958:        if self._data_list and 0 <= self._data_index < len(self._data_list):
1961:            if row_vals:
1963:                    if idx < len(row_vals):
1969:        if self._data_row_values is not None and 0 <= self._data_index < len(self._data_row_values):
1976:            if isinstance(val, (int, float)):
1985:        if a_num is not None and b_num is not None:
1990:        if op == ">":
1992:        if op == "<":
1994:        if op == ">=":
1996:        if op == "<=":
1998:        if op == "!=":
