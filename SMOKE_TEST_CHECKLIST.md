# 실사용 스모크 테스트 체크리스트

목적: 자동 테스트 통과 이후, 실제 화면/해상도/창 상태에서 기능이 의도대로 동작하는지 최종 확인한다.

## 0. 사전 자동 게이트
1. `python run_smoke_suite.py --quick`
2. `python run_smoke_suite.py`
3. 두 명령 모두 `PASS`인지 확인

실패하면 수동 점검 전에 해당 실패를 먼저 수정한다.

## 1. 테스트 환경 고정
1. 테스트 대상 앱(게임/업무앱) 창 크기와 위치를 고정한다.
2. 윈도우 배율(DPI), 해상도, 멀티모니터 구성을 실제 운영 환경과 동일하게 맞춘다.
3. 매크로 앱을 운영과 같은 권한(일반/관리자)으로 실행한다.
4. 로그 폴더(`logs`)의 최신 파일명을 기록한다.

## 2. 핵심 기능 수동 시나리오

### A0. 타겟 셀렉터
1. 상단 `...` 버튼으로 창 선택기를 연다.
2. 목록이 표시되는지 확인하고, 임의 창 1개를 선택해 Target 입력칸 반영을 확인한다.

Pass 기준:
- 빈 다이얼로그가 아니어야 함(최소 안내 문구 또는 실제 항목 표시).
- 선택 후 Target 입력칸에 창 제목이 반영되어야 함.

### A. 캡처 -> 매칭
1. `Add Image`로 목표 UI를 다시 캡처한다.
2. `Test Match on Screen` 실행.
3. 결과가 `OK: conf=...`로 나오고 위치가 실제 대상과 맞는지 확인.

Pass 기준:
- `Error`/`No match`가 없어야 함.
- 같은 화면에서 3회 반복 시 3회 모두 성공.

### B. 매칭 -> 클릭
1. A에서 만든 스텝 액션을 `click`으로 설정.
2. `Run` 1회 실행.
3. 대상 UI가 실제 클릭 반응(선택/열림/토글)을 보이는지 확인.

Pass 기준:
- 오클릭(다른 위치 클릭) 0회.
- 클릭 후 의도된 UI 상태 변화가 발생.

### C. 분기(TRUE/FALSE)
1. `jump_if` 또는 `image_branch` 시나리오를 준비한다.
2. TRUE가 나오는 상태에서 실행해 TRUE 경로 이동 확인.
3. FALSE가 나오는 상태에서 실행해 FALSE 경로 이동 확인.

Pass 기준:
- TRUE/FALSE 각각 1회 이상 기대 경로로 이동.
- 잘못된 경로 이동 0회.

### D. 반복(loop_until_hide 또는 repeat)
1. 반복 스텝을 1개 포함한 시나리오 실행.
2. 반복 중지 조건(이미지 사라짐/반복 횟수 도달)이 정상 동작하는지 확인.

Pass 기준:
- 무한 반복/조기 종료 없이 조건대로 종료.
- 종료 후 앱 상태가 정상(버튼 상태, UI 응답) 유지.

### E. 트리거/스케줄러(사용 시)
1. 트리거 조건을 만족시키고 자동 실행 여부 확인.
2. 스케줄러 예약 시간 도달 시 실행 여부 확인.
3. 실패 정책(`continue/stop/retry`)이 설정대로 동작하는지 확인.

Pass 기준:
- 트리거/스케줄러 시작 이벤트가 로그에 기록.
- 실패 정책이 설정값과 일치.

## 3. 로그 검증 포인트
1. `Starting macro...` 로그 존재
2. 분기/반복 관련 경고/예외 없음
3. 종료 시 비정상 trace 없음

## 4. 결과 기록 템플릿

실행일:
환경(해상도/DPI/권한):
테스트 매크로 파일:
로그 파일:

- A 캡처->매칭: PASS/FAIL
- A0 타겟 셀렉터: PASS/FAIL
- B 매칭->클릭: PASS/FAIL
- C 분기(TRUE/FALSE): PASS/FAIL
- D 반복 제어: PASS/FAIL
- E 트리거/스케줄러: PASS/FAIL 또는 N/A

실패 항목 상세:
- 재현 절차:
- 기대 결과:
- 실제 결과:
- 관련 로그 라인:
